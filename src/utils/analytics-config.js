// Generated for configured builds by `npm run configure:analytics`.
// These values are extractable from the extension ZIP, including apiSecret.
// A source checkout sends nothing until both values have been configured.
export const ANALYTICS_CONFIG = Object.freeze({
  measurementId: '',
  apiSecret: '',
  debug: false
});
